# ============================================================
# CORE REQUIREMENTS (CPU-ONLY OCR PIPELINE – LEAN)
# #######  IMPORTANT ###### Install PyTorch (CPU wheel) separately BEFORE this file:
#   pip install --no-cache-dir --index-url https://download.pytorch.org/whl/cpu torch==2.2.0
# Then:
#   pip install --no-cache-dir -r requirements.txt
# ============================================================

# --- Web UI ---
streamlit==1.38.0

# --- Image & PDF (choose ONE backend initially) ---
Pillow==10.4.0
pymupdf==1.24.9            # Primary PDF rasterization / text extraction
# pdf2image==1.17.0        # OPTIONAL: enable only if you specifically need poppler-based rendering

# --- TrOCR & NLP Core ---
transformers==4.42.4
sentencepiece==0.2.0
numpy==1.26.4
rapidfuzz==3.9.3

